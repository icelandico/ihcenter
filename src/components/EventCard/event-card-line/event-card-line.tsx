import * as React from "react"
import { rootStore } from "../../../store/RootStore"
import {} from "./event-card-line-styles"

interface Props {
  store?: typeof rootStore
}

// const EventCardLine: React.FC<Props> = props => { }